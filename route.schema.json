{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "TransitJSON Route",
  "type": "object",
  "properties": {
    "route_id": {
      "type": "string"
    },
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "URL-safe benzersiz tanımlayıcı, örn. 'bur-91'. API rotalarında kullanılır: /api/routes/{slug}"
    },
    "agency_id": {
      "type": "string"
    },
    "name": {
      "type": "object",
      "properties": {
        "tr": { "type": "string" },
        "en": { "type": "string" }
      },
      "required": [
        "tr"
      ],
      "additionalProperties": {
        "type": "string"
      },
      "description": "Çoklu dil isim nesnesi. 'tr' zorunludur, diğer dil kodları (en, ar, de vb.) opsiyoneldir."
    },
    "code": {
      "type": ["string", "null"]
    },
    "color": {
      "type": ["string", "null"],
      "pattern": "^#[0-9A-Fa-f]{6}$"
    },
    "vehicle_type": {
      "type": "string",
      "enum": [
        "bus",
        "tram",
        "metro",
        "rail",
        "ferry",
        "cable_tram",
        "gondola",
        "funicular",
        "trolleybus",
        "monorail",
        "minibus",
        "coach",
        "water_taxi"
      ],
      "description": "Araç türü. bus: otobüs, tram: tramvay, metro: metro/subway, rail: demiryolu (Marmaray/banliyö/YHT), ferry: vapur/feribot, cable_tram: kablolu tramvay, gondola: teleferik/gondol, funicular: füniküler, trolleybus: troleybüs, monorail: monoray, minibus: minibüs/dolmuş, coach: şehirlerarası otobüs, water_taxi: deniz taksi/motor."
    },
    "fare_id": {
      "type": ["string", "null"],
      "description": "Bu hat için geçerli ücret tanımı. fares.json'daki fare_id'ye referans verir. Opsiyonel/null — yoksa bu hat için tanımlı özel bir ücret yok kabul edilir."
    },
    "route_pattern": {
      "type": "string",
      "enum": [
        "round_trip",
        "loop"
      ],
      "description": "Geriye uyumlu hat topolojisi sınıflandırmasıdır; route_patterns koleksiyonundaki güzergâh varyantı ile karıştırılmamalıdır. round_trip: genel olarak çift yönlü hat, loop: genel olarak ring hat. Bir hattın farklı varyantları için kesin loop bilgisi route_patterns.is_loop alanındadır."
    },
    "stop_mode": {
      "type": "string",
      "enum": [
        "fixed",
        "flexible"
      ],
      "description": "fixed: tüm ara duraklar tanımlı. flexible: sadece ilk/son durak tanımlı, arası serbest biniş."
    },
    "updated_at": {
      "type": "string",
      "format": "date-time"
    },
    "source": {
      "type": "string"
    }

  },
  "required": [
    "route_id",
    "slug",
    "agency_id",
    "name",
    "vehicle_type",
    "route_pattern",
    "stop_mode",
    "updated_at"
  ],
  "additionalProperties": false
}
